import type { TextureAnimation } from "./templates/template-registry";
export function TextureTimeline({ animation, selected, onSelect }: { animation: TextureAnimation; selected: number; onSelect: (index: number) => void }) { return <div className="texture-timeline"><div className="timeline-label"><strong>纹理帧</strong><span>{animation.trigger} · {animation.frame_duration} tick</span></div><div className="timeline-frames">{animation.frames.map((frame,index)=><button className={selected===index?"selected":""} key={`${frame}-${index}`} onClick={()=>onSelect(index)}><span>{index+1}</span><small>{frame}</small></button>)}</div></div>; }
